领导对你上一版规范的评价只有四个字：**不堪一击**。
他说，如果连这种规范都觉得严格，那你就别碰数据了，去填表吧。

下面这版是 **“魔鬼领导特批·地狱成长版”**，看完你要是手心不出汗，算我输。

---

# 数据分析练习项目成长规范（汽车销量·地狱版）

> **领导原话**：“我要的不是学生作业，是一个能直接上董事会汇报、抗得住我连续 30 个‘为什么’的商业分析武器。现在，把上一个软绵绵的版本扔进回收站，按这个来。”


## 一、项目定位与灵魂拷问

**项目名称**
中国乘用车市场销量增长深度解剖（Car Sales Deep-Dive）

**项目核心目标（刻进脑子里）**
1. **增长策略必须用钱说话**：你的每条建议，我都要看到预估的营收影响区间和投入等级。没有？那就是废话。
2. **数据不是支撑，数据是镣铐**：每个结论必须附带“数据-逻辑-结论”的铁链。你敢说出“大概”“可能”“我觉得”这种词，直接撕掉重写。
3. **图表必须会说话，而且只能说真话**：每一张图必须能用一句话讲清楚商业发现，否则就是废图。
4. **你的代码必须经得起“断网测试”**：关掉 AI，关掉搜索引擎，当着我的面改参数并解释逻辑。做不到？项目不算完成。
5. **消费者行为是你最深的护城河**：任何数字波动，必须先想“人”怎么了。只看数字不看人，叫做会计，不叫分析师。
6. **自我怀疑是必修课**：每完成一步，必须写下至少三项“我可能哪里错了、哪里漏了、哪里会被领导锤爆”，否则不准进入下一步。

> **领导附加条款**：本次项目中，你要默认自己面对的是一个会在凌晨两点打电话问“你第三页那个 7% 的环比增长是真增长还是春节效应”的疯子。请做好心理准备。


## 二、核心分析流程（禁止跳过任何一步）

你的工作流必须严格按照以下链条进行，并留下每一步的 **“思考遗迹”**：

**商业假设 → 数据侦察 → 逻辑链构建 → 异常猎杀 → 决策预演 → 自我推翻**

每份分析文档开头，都必须先列出 **本次分析试图推翻的 3 个假设**。
例如：
- 假设 1：广东省销量高纯粹因为人口多，没有车型偏好差异。
- 假设 2：SUV 的增长完全是市场趋势，与政策无关。
- 假设 3：12 月的销量高峰只是因为年底促销。

分析结束时，你要回过头来宣布：哪条假设被你判了死刑，哪条仍然悬而未决，哪条你差点被数据骗了。


## 三、项目目录结构（多一样累赘，少一样缺胳膊）

```
project_root/
├── data/
│   ├── raw/                # 原始数据，动一个字节就给我走人
│   ├── processed/          # 清洗后数据，每个文件必须有对应的清洗日记
│   └── external/           # 你爬取或引用的外部数据（如政策、天气、节假日）
├── charts/
│   ├── exploration/        # 探索性图表，逻辑可能粗糙
│   └── final/              # 经得起推敲、可直接放汇报的图
├── notebooks/
│   ├── 01_data_autopsy.ipynb   # 数据尸检报告
│   ├── 02_behavior_hunt.ipynb  # 行为线索挖掘
│   ├── 03_growth_engine.ipynb  # 增长引擎拆解
│   └── 04_final_report.ipynb   # 最终汇报文件
├── reports/
│   ├── project_report.md       # 面向董事会的正式报告
│   ├── reflection_log.md       # 你的噩梦记录本（犯错、修正、领导刁难）
│   └── executive_summary.txt   # 给大老板看的 200 字极简版
├── src/
│   └── utils.py                # 你可复用的函数，必须有完整 docstring
└── README.md                   # 武器使用说明书
```

**违反目录规范**：视为项目未启动。


## 四、代码规范（不流血不通过）

1. **注释必须说出商业动机，不许翻译代码**
   ```python
   # ❌ 领导会撕掉的写法：
   # 按省份和月份分组求和
   df.groupby(['province','month'])['sales'].sum()

   # ✅ 能活下来的写法：
   # 目的：捕捉不同省份的月度销售节奏，寻找是否存在“政策市”或“返乡购车潮”等行为信号，
   # 为后续的差异化库存调配建议打地基。
   monthly_province_sales = df.groupby(['province','month'])['sales'].sum()
   ```

2. **每个分析模块必须是一个可单独运行的单元**
   禁止一堆代码从头跑到尾。每个分析主题封装成函数，并附带 **assert 检查**。
   ```python
   def analyze_seasonal_pattern(df):
       """
       识别各车型的季节性强弱，区分自然季节性与政策/促销驱动。
       返回 DataFrame，包含 seasonal_index 及强/弱标签。
       """
       # ... 逻辑 ...
       assert 'seasonal_index' in result.columns, "缺少关键列，无法支撑后续建议"
       return result
   ```

3. **“四问”升级为“铁血五问”**
   每段分析代码前，必须在 notebook 中用 markdown 回答，缺一不可：
   | 问题 | 回答要求 |
   |---|---|
   | 分析什么？ | 精确到变量、维度、时间窗口 |
   | 为什么分析能指向增长？ | 必须阐述与营收/市场份额/用户价值的关联 |
   | 使用了哪些数据字段及清洗规则？ | 逐一列出，并说明空值、异常值处理 |
   | 得到的初步结论及置信度自评？ | 用高/中/低，并写出判断依据 |
   | 这个结论最容易被攻击的点是什么？ | **（新增）** 至少写出一条，否则视作思考不完整 |


## 五、数据处理规范（每步处理都是呈堂证供）

1. **建立“数据尸检报告”**
   在 `01_data_autopsy.ipynb` 中，必须包含：
   - 字段级缺失率与缺失机制推断（MCAR/MAR/MNAR，别只写个百分比）
   - 异常值猎杀清单：IQR、Z-score、业务逻辑三重判定
   - 重复值：是系统错误，还是同一天同一车型的多笔真实交易？必须给出你的侦查结论。

2. **处理日志不可磨灭**
   任何被删除、修改、插补的行，必须输出到 `data/processed/operation_log.csv`，包含：
   `时间戳 | 操作 | 影响行数 | 业务理由 | 风险评估`
   领导会随机抽一条问你：“这条为什么不是保留为异常分析？”答不上来就是态度问题。

3. **保留未清洗的“对照副本”**
   在 processed 目录下，必须同时存在一份仅做了类型转换、未做任何过滤的 `raw_mirror.csv`，用以随时回溯你的每一步决策。


## 六、可视化规范（领导的眼睛是显微镜）

1. **每张图必须有死刑判词般的标题**
   禁止使用“各省销量柱状图”这种废物标题。
   必须用 **结论式标题**，例如：
   `广东销量一骑绝尘，但第二梯队三省增速已连续3个月超越其1.2倍`

2. **图表标注如手术刀**
   - 所有异常峰、谷、断点，必须用**箭头+文字**直接在图上标注可能原因（如：2025.02 低谷 → 春节工作日减少 + 新能源补贴空窗期）。
   - 趋势线必须标明 R²，并在一行注释里说清楚你为什么不直接相信它。

3. **视觉铁律**
   - 只能用**单色系或灰度**，颜色不超过 3 种，用于突出关键差异。
   - 禁止任何 3D 效果、阴影、渐变背景。
   - 字型统一为无衬线，标题加粗，Y轴从0开始（除非你有足够充分的理由，并在图下写出辩词）。
   - 所有数值保留两位小数，增长率务必带上**百分比符号**。

4. **制图后的“自残检查”**
   每放一张图，你必须在下方面对三个问题：
   - 如果遮住标题，读者能在 5 秒内看懂核心信息吗？
   - 这张图有没有隐藏任何可能打脸的负面信息？
   - 换成另一种图表类型，发现的事实会不会变？


## 七、分析报告规范（能直接发给投资人的那种）

报告必须包含 **五** 部分（在原本四部分基础上增加“反思”章节）：

**1. 项目背景**
- 必须交代：本次分析是基于哪家企业的视角（车企/经销商集团/咨询公司），因为立场决定策略。
- 数据规模及时间跨度，必须明确指出数据**不具备代表性的可能风险**（如：仅包含上险量而缺零售量，缺失平行进口等）。

**2. 分析方法**
- 必须画一张**分析逻辑流图**（可用 mermaid 或手绘图），让门外汉看明白你的推导路径。
- 清洗方法必须详述，尤其是被剔除的异常值，你要说明为什么它们是异常而不是“新物种”。

**3. 分析结果（魔鬼细节章）**
- 每个发现必须遵循：**商业问题 → 关键图表 → 数据解读 → 行为/心理归因 → 增长杠杆提示**
- 所有增长或下降，必须拆解为**存量迁移**、**新增需求**、**替代效应**三个维度去解释。
- 必须单独辟一节“异常现象猎杀报告”，将你抓住的每一个怪异数字进行审讯。

**4. 增长建议与风险预案**
- 每条建议必须包含：**目标区域/车型、执行动作、所需资源等级、预期拉动效果、最大风险与退出条件**。
- 必须画一张“建议优先级矩阵”，以**潜在收益**和**执行复杂度**为轴，将你的建议投放上去。

**5. 诚实反思与认知局限（新增必写章）**
- 本次分析中，你最大的三个认知盲区是什么？
- 如果有更多数据（如消费者画像、竞品定价、舆情），你最想要什么？为什么？
- 你在这份报告里最心虚的一个结论是什么？现在给你 30 秒，你怎么向领导坦白？


## 八、成长型学习规范（每晚必须做的噩梦）

1. **“领导附体”自问清单**
   每天结束前，打开 `reflection_log.md`，依次回答：
   - 我今天做的哪个分析，换个时间窗口可能结论全变？
   - 如果领导现在问我“这数是不是你为了好看才这么算的”，我该怎么证明自己清白？
   - 我有没有因为某个图表漂亮，而潜意识里忽略了它背后数据的肮脏？

2. **“推翻重来”时刻**
   在项目中期，强制自己至少做一次：**把最重要的那个结论完全推翻**，站在对立面找证据。如果你不能打败自己，领导就会打败你。

3. **“一句话”训练**
   每完成一个分析模块，必须把它浓缩成**只有一句话的、能对着电梯里的 CEO 讲清楚的洞见**。如果做不到，说明你自己还没想透。

4. **地狱级提问库（随机抽检）**
   领导可能随时问你，请确保你能回答：
   - “你这个增长率，扣掉通货膨胀/补贴退坡还剩多少？”
   - “广东销量高，是不是只是因为 4S 店多？你有店均销量数据吗？”
   - “你把 MPV 归为增长型，是不是因为去年基数太低？给我看定基比。”
   - “图上这个突刺，是统计错误还是某个大客户团购？你怎么验证？”
   - “你的建议要花多少钱？如果只能选一条做，放弃哪两条？为什么？”
   ……（请自行脑补更多，领导不会只问五个）


## 九、最终验收标准（活过这场答辩才算完）

- 你必须站在一个空会议室里，仅靠纸质版图表和一支马克笔，向“扮演领导”的同伴讲清整个分析，并扛住 20 个以上的追问。
- 你的所有代码、数据、图表、报告，必须能在一个**全新的虚拟环境**中，通过 `python main.py` 或 `run_all.sh` 一键复现全部结果。
- 答辩结束时，你要交出一份 **“如果我多三个月时间，我会怎么做”** 的后续研究计划，否则视为没有商业野心。


**现在，忘掉你之前见过的所有温和规范。这份，才是你那严苛领导真正期待的东西。去干活，别让我看见你手抖。**
